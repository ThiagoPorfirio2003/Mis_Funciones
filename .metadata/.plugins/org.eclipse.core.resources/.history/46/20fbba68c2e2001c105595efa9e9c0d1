/*
 * memoriaDinamica.c
 *
 *  Created on: 2 jun. 2022
 *      Author: thiag
 */

#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int* newInt()
{
	int* newInt;
	newInt = NULL;

	newInt= (int*) malloc(sizeof(int));

	return newInt;
}

float* newFloat()
{
	float* newFloat;
	newFloat = NULL;

	newFloat= (float*) malloc(sizeof(float));

	return newFloat;
}

char* newChar()
{
	char* newChar;
	newChar = NULL;

	newChar= (char*) malloc(sizeof(char));

	return newChar;
}

int dimensionarInt(int* punteroADimensionar, int nuevaLongitud)
{
	int retorno;
	int* bufferInt;

	bufferInt = NULL;
	retorno =1;

	if(punteroADimensionar !=NULL && nuevaLongitud > 0)
	{
		bufferInt= (int*) realloc(punteroADimensionar, sizeof(int)* nuevaLongitud);
		if(bufferInt != NULL)
		{
			punteroADimensionar = bufferInt;
			retorno =0;
		}
	}
	return retorno;
}

float dimensionarInt(float* punteroADimensionar, int nuevaLongitud)
{
	int retorno;
	float* bufferInt;

	bufferInt = NULL;
	retorno =1;

	if(punteroADimensionar !=NULL && nuevaLongitud > 0)
	{
		bufferInt= (float*) realloc(punteroADimensionar, sizeof(float)* nuevaLongitud);
		if(bufferInt != NULL)
		{
			punteroADimensionar = bufferInt;
			retorno =0;
		}
	}
	return retorno;
}
